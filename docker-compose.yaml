services:
  web:
    build: .
    command: >
      bash -c "python manage.py migrate --noinput && python manage.py csu && python manage.py add_payments && python manage.py collectstatic --noinput && gunicorn drf_hw.wsgi:application --bind 0.0.0.0:8000"
    environment:
      - DEBUG=False
      - DATABASE_HOST=db
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
    expose:
      - "8000"
    depends_on:
      - db
      - redis
    env_file:
      - ./.env

  nginx:
    build:
      context: ./nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - static_volume:/app/staticfiles
    depends_on:
      - web

  db:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env

  redis:
    image: "redis:7"
    volumes:
      - redis_data:/data
    env_file:
      - ./.env

  celery:
    build: .
    command: >
      bash -c "celery -A drf_hw  worker --loglevel=info --pool=eventlet"
    ports:
      - "6379:6379"
    env_file:
      - ./.env
    depends_on:
      - db
      - redis

  celery_beat:
    build: .
    command: >
      bash -c "celery -A drf_hw beat --loglevel=info"
    env_file:
      - ./.env
    volumes:
      - .:/app
    depends_on:
      - db
      - redis
      - celery

networks:
  default:

volumes:
  postgres_data:
    driver: local
  static_volume:
    driver: local
  redis_data:
    driver: local

